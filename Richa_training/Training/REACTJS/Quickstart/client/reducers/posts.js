export default function posts(defStore=[],action){

    switch(action.type){
        case 'RATE_POST':
        return defStore;
        default :
        return defStore;
 
     }

    console.log("Within posts... reducers");
    return defStore;
}